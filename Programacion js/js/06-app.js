let c;
let f;

c = parseFloat(prompt("Digita el valor en grados celcius"))

f = (c * (9 / 5)) + 32

document.write (`los ${c} grados celcius convertidos a fahrenheit son: ${f} `)
console.log(`los ${c} grados celcius convertidos a fahrenheit son: ${f} `)
